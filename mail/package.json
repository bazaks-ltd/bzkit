{
  "name": "mail",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "haraka": "haraka",
    "mail:run": "haraka -c ./outbound",
    "mail:test": "swaks --to terence@bazaks.com --from test@example.com --server localhost --port 587 --auth-user user1 --auth-password password1 -header 'Subject: test'"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "dependencies": {
    "Haraka": "^3.0.2"
  }
}
